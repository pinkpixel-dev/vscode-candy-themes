# Roadmap

## Near term

- Capture refreshed screenshots after the current Candy Themes polish pass so the README and Marketplace listing reflect the updated palettes.
- Publish a fresh `.vsix` after smoke-testing the new `npm run package` workflow end to end.
- Verify the live Marketplace listing after the updated logo, icon, and README layout are published.
- Keep refining per-theme workbench chrome so tabs, sidebars, panels, quick input, and terminal colors feel as intentional as the editor syntax.
- Decide whether the archived `other/` source folders should stay in the repo or be moved out entirely now that they are excluded from packaging.
- Add CI to run `npm run validate` on every push and pull request.

## Later

- Add a few new candy-adjacent variants without overlapping too closely with the Sweet Themes collection.
- Add a theme preview page on the Pink Pixel site for both VS Code theme packs.
- Explore companion icon-theme or accent-pack ideas for the Pink Pixel editor ecosystem.
